[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "search-library"
version = "0.1.0"
description = "Generic search algorithms"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]